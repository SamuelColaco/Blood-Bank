import { Controller } from '@nestjs/common';

/**
 * HTTP entry points for the Donation & Screening bounded context.
 * Controllers will be added here as use cases are implemented.
 */
@Controller('donation')
export class DonationController { }
